import {cp,mkdir,readFile,rm,writeFile} from 'node:fs/promises';import {stripTypeScriptTypes} from 'node:module';
const out=new URL('../dist/',import.meta.url);await rm(out,{recursive:true,force:true});await mkdir(new URL('assets/',out),{recursive:true});let html=await readFile(new URL('../index.html',import.meta.url),'utf8');await writeFile(new URL('index.html',out),html.replace('./src/app.ts','./assets/app.js'));
const names=['app','config','storage','architecturePages','networkArchitecture','learningInteractions','physicalPages','physicalNetwork','ethernetPages','ethernetNetwork','ipv4Pages','ipv4Network','subnetPages','subnetNetwork','routingPages','routingNetwork','transportPages','transportNetwork','servicesPages','servicesNetwork','constructorPages','constructorNetwork'];
for(const name of names){let source=stripTypeScriptTypes(await readFile(new URL(`../src/${name}.ts`,import.meta.url),'utf8'),{mode:'strip'});for(const dep of names)source=source.replaceAll(`'./${dep}'`,`'./${dep}.js'`).replaceAll(`'./${dep}.ts'`,`'./${dep}.js'`);for(const css of ['style','learningInteractions','physical','ethernet','ipv4','subnet','routing','transport','services','constructor','statistics'])source=source.replace(`import './${css}.css';`,'');await writeFile(new URL(`assets/${name}.js`,out),source)}
const css=['style','learningInteractions','physical','ethernet','ipv4','subnet','routing','transport','services','constructor','statistics'];await writeFile(new URL('assets/style.css',out),(await Promise.all(css.map(x=>readFile(new URL(`../src/${x}.css`,import.meta.url),'utf8')))).join('\n'));await cp(new URL('../public/manifest.webmanifest',import.meta.url),new URL('manifest.webmanifest',out));await writeFile(new URL('.nojekyll',out),'');console.log('Production build created in dist/');
