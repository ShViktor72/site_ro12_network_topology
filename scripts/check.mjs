import {readFile} from 'node:fs/promises';import {stripTypeScriptTypes} from 'node:module';
const names=['config','storage','networkArchitecture','architecturePages','physicalNetwork','physicalPages','ethernetNetwork','ethernetPages','ipv4Network','ipv4Pages','subnetNetwork','subnetPages','routingNetwork','routingPages','transportNetwork','transportPages','servicesNetwork','servicesPages','constructorNetwork','constructorPages','learningInteractions','app'];
for(const name of names){const file=`src/${name}.ts`,source=await readFile(file,'utf8');stripTypeScriptTypes(source,{mode:'strip'});console.log(`OK ${file}`)}
